#include <iostream>
#include <string>

class DisplayMessage {
public:
    // Constructor
    DisplayMessage() {
        std::cout << "Object created! Displaying a simple message." << std::endl;
    }
};

int main() {
    // Creating an object of DisplayMessage class
    DisplayMessage myObject;

    // You can create more objects, and each time an object is created, the message will be displayed
    DisplayMessage anotherObject;

    return 0;  // in the name of the allah the most computnat3 in the name of this 
}
